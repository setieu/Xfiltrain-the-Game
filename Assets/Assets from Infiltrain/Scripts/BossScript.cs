using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BossScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //doesnt need many if statements because wont spawn unless case 5 (in game manager)
        //still needs gameActive if statements
    }

    // Update is called once per frame
    void Update()
    {
        //shoot missles every so often
    }
}
